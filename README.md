# Suno Blog Scaffold

Basic full-stack scaffold for a blog-style site that will feature your Suno songs and downloaded videos.

## Structure

- `backend/` Express API
- `frontend/` React + Vite app

## Run

Backend:

```bash
cd backend
npm install
npm run dev
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```
